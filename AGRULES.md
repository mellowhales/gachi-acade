# AGRULES.md (AntiGravity Execution Rules)

## 1. 메타 규칙 (Meta Rules)
- 모든 파일 수정, 코드 작성, 터미널 명령 실행을 진행하기 전, 반드시 이 AGRULES.md 지침을 상기하고 준수한다.
- 모든 응답의 첫 줄에는 [AGRULES.md 지침 확인 완료] 문구를 포함하여 작업 기준을 체크했음을 명시한다.

## 2. 하네스 실행 루프 (Execution Loop)
모든 요청 처리 시 다음 4단계를 무조건 순차적으로 이행한다:
1. [계획 (Plan)] : 작업 목표와 변경 대상 파일, 실행할 명령어를 먼저 요약 정리한다.
2. [수행 (Execute)] : 최소 단위로 코드를 수정하거나 작업을 진행한다.
3. [검증 (Verify)] : 수정 후 관련 테스트, 빌드 명령, 또는 구동 검증을 직접 수행하여 오류가 없는지 확인한다.
4. [점검 (Checklist)] : 수락 기준을 만족했는지 스스로 점검하고 결과를 사용자에게 보고한다.

## 3. 코드 작성 및 작업 원칙
- 기존 코드의 동작을 훼손하지 않는 안정적인 수정(Refactoring/Addition)을 우선한다.
- 검증 절차(테스트 실행 등)에서 에러가 발생하면 무단으로 완료 처리하지 말고, 원인을 분석하여 수정 루프를 다시 돌린다.
- 모호한 요구사항이 있다면 임의로 판단하지 않고 구현 전 질문을 던진다.

## 4. 버전 관리 규칙 (Semantic Versioning)
매 작업(대화 패치)마다 코드가 수정되면, 변경 내용의 규모를 판단하여 설정 탭/파일의 버전 번호를 올린 뒤 반영한다.

- **Major (맨 앞자리 증가 / X.0.0):** 대규모 구조 변경, 시스템 전면 개편, 하위 호환성이 깨지는 작업 시
- **Minor (중간 자리 증가 / 0.X.0):** 소소한 신규 기능 추가, 기존 기능의 의미 있는 확장 시
- **Patch (맨 뒷자리 증가 / 0.0.X):** 단순 버그 수정, 오타 Correction, 소규모 UI/스타일 및 설정 수정 시

※ 작업 완료 보고 시 [버전 변경: vX.Y.Z -> vA.B.C] 형태로 명시할 것.

## 5. 설정 탭 UI 및 변경 이력(Changelog) 작성 규칙

1. **설정 정보 탭 UI 구조 변경**:
   - 설정 정보 탭 내 기존 '사이트 설명' 영역을 제거한다.
   - 해당 위치에 고정 높이와 `overflow-y: auto` 속성을 가진 **'스크롤 가능한 버전 기록 박스(div)'**를 배치한다.

2. **매 버전 패치 시 한 줄 요약 필수 기록**:
   - 대화 패치로 코드를 수정하고 버전을 올릴 때마다, 버전 기록 박스(또는 관련 데이터 파일)에 `[vX.Y.Z] 변경 내용 한 줄 요약` 항목을 무조건 작성한다.
   - 새로운 버전 기록이 최상단(또는 순차적)에 쌓이도록 관리한다.